import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'club.smartsheep.health.eatingsafety',
  appName: 'EatingSafety',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
